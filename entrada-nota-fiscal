const fs = require('fs');
const path = require('path');

const distEntry = path.resolve(__dirname, 'dist/modules/entrada-nota-fiscal/server.js');
const srcEntry = path.resolve(__dirname, 'src/modules/entrada-nota-fiscal/server.ts');

if (fs.existsSync(distEntry)) {
  require(distEntry);
} else {
  require('tsx/cjs');
  require(srcEntry);
}
